version: 1.0.{build}
configuration: Release
install:
- ps: '#sleep 20'
before_build:
- cmd: nuget restore -verbosity quiet
build:
  publish_wap: true
  verbosity: quiet
test: off
deploy:
- provider: Environment
  name: IIS1
